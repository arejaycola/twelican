import React from 'react';

const Search = () => {
	return (
		<>
			<input placeholder="Enter a verified Twitter Account" type="text" className="user-search" />
			<button>Search</button>
		</>
	);
};
export default Search;
